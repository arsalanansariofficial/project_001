generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model url {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  full_url  String @unique
  short_url String @default(cuid())
}
